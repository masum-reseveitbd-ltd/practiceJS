// ceil mean upper number
//math.abc ()
//round nerest intager
//random number
//math floor

// const number = -5;
// const output = Math.abs(number);
// console.log(output);
// --------------------------------------------------
// function abs(x, y) {
//   return Math.abs(x + y);
// }

// let number = abs(10, 23);       //The Math.abs() function returns the absolute value of a number. That is, it returns x if x is positive or zero, and the negation of x if x is negative.
// console.log(number);
// --------------------------------------------------
// const ceilNumber = 4.4532;
// const outputCeil = Math.ceil(ceilNumber); //The Math.ceil() function always rounds a number up to the next largest integer.
// console.log(outputCeil);

// const number = 5.554; //helf of number then rounded the number=5.556 rounded 6 but 5.456 rounded 5 thats all .
// const rounded = Math.round(number);
// console.log(rounded);
// ----------------------------------------------------------------
const rendomNumber = Math.random() * 6;
const output = Math.round(rendomNumber);
console.log(output);
